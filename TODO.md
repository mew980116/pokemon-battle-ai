# 待办工作

## 文档完善
1. 根据用户提供的回调函数使用方法，进一步完善 `docs/api/callback-functions.md`
2. 整理 `battle` 对象方法的参数和返回值的详细说明
3. 编写 `battle` 对象方法的使用示例，帮助开发者更好地理解和使用这些方法
4. 分析 `weather` 和 `terrain` 属性的含义和可能的取值范围
5. 整理 `battle.data.field.zone` 对象的属性和方法，分析其在战斗中的影响

## 功能开发
1. 将 `20201227.js` 中的核心逻辑逐步迁移到 `src/core/battleAI.js`
2. 导入 `move_message.txt` 等参考文件，完善消息编号与实际道具/技能/特性的映射关系

## 数据探索
1. 进一步探索 `battle` 对象的对战操作相关方法的具体用法
2. 记录更多与战斗AI决策相关的 `battle` 对象方法的使用案例
3. 分析这些方法在战斗AI对战决策过程中的应用场景
4. 深入研究 `battle.data.field` 对象的属性和方法，获取对战场地相关信息
5. 分析 `battle.data.team(int)` 方法的返回值结构，了解队伍数据的组织方式
6. 探索 `battle.data.avatar(int)` 方法的返回值，了解头像相关信息
7. 研究 `battle.data.field.poke(int)` 方法的返回值结构，获取宝可梦详细数据
8. 探索 `battle.data.field.poke(int).pokemon` 对象的属性和方法，获取宝可梦的详细信息
9. 探索 `battle.data.field.poke(int).pokemon.move(int)` 方法的返回值，了解宝可梦技能信息
10. 深入研究 `battle.data.field.zone(int)` 方法的返回值，了解对战区域的陷阱和状态信息

## 代码优化
1. 优化 `20201227.js` 中的代码结构，提高可读性和可维护性
2. 实现模块化设计，将不同功能的代码分离到不同的文件中
3. 添加代码注释，提高代码的可读性

## 测试与调试
1. 完善 `test_callbacks.js` 测试脚本，增加更多测试场景
2. 测试 `battle` 对象的各种方法，确保其在不同场景下的正确使用
3. 调试可能存在的问题，确保代码的稳定性

## 项目管理
1. 更新 `README.md`，添加项目的最新进展和使用说明
2. 整理项目文档，确保文档的完整性和准确性
3. 制定项目开发计划，明确后续的开发目标和时间节点
