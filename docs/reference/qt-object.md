# Qt 对象参考文档

本文档记录了从宝可梦战斗AI系统运行环境中读取到的Qt对象的属性和方法。

## Qt 对象属性

Qt 对象没有直接的属性，主要包含以下方法。

## Qt 对象方法

| 方法名 | 描述 |
|-------|------|
| `lighter()` | 使颜色变亮的函数 |
| `darker()` | 使颜色变暗的函数 |
| `lightness()` | 获取颜色亮度的函数 |
| `tint()` | 给颜色添加色调的函数 |

## 读取方法

```javascript
// 读取Qt对象的所有属性
for(var prop in Qt){if(Qt.hasOwnProperty(prop)){print(prop+": "+Qt[prop]);}}
```

## 注意事项

- 本文档基于实际运行环境中读取的结果，不同环境可能会有所差异
- 某些属性和方法可能需要特定的权限或条件才能访问
- 在使用这些属性和方法时，请注意兼容性问题，避免使用现代JavaScript特性
- Qt对象主要包含颜色处理相关的方法，与宝可梦对战的核心逻辑关联不大
- 这些方法可能主要用于UI界面的颜色处理，而非对战逻辑

## 后续工作

- 进一步探索Qt对象的具体属性和方法
- 记录更多与Qt框架相关的功能
- 分析这些属性和方法在战斗AI中的应用场景
